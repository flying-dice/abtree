description: |
  A cyclic $ref is preserved as a placeholder in the snapshot rather
  than expanded, so execution creation does not stack-overflow. Ticking the
  cyclic edge surfaces a clean failure.
tree: cycle-root
files:
  cycle-root.yaml: |
    name: cycle-root
    version: 1.0.0
    state:
      local: {}
    tree:
      type: sequence
      name: Cycle_Root
      children:
        - $ref: "#/tree"
steps: []
final:
  status: failure

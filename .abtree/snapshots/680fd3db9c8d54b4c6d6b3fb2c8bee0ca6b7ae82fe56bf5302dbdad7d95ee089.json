{
  "local": {
    "test_path": null,
    "test_spec": null,
    "target_execution_id": null,
    "final_local": null,
    "final_status": null,
    "mermaid_diagram": null,
    "assertions": null,
    "overall_result": null,
    "report_path": null
  },
  "global": {},
  "root": {
    "type": "sequence",
    "name": "Test_Runner",
    "children": [
      {
        "type": "action",
        "name": "Load_Spec",
        "steps": [
          {
            "kind": "evaluate",
            "expression": "$LOCAL.test_path is set"
          },
          {
            "kind": "instruct",
            "instruction": "Read the YAML file at $LOCAL.test_path. Parse it and store the parsed object at $LOCAL.test_spec. The parsed object MUST contain at minimum: `tree` (slug of the tree under test), `scenario.name`, `scenario.given`, `scenario.when`, `scenario.then`. If any of those are missing or unreadable, submit failure — a malformed spec is not a test the runner can drive."
          }
        ]
      },
      {
        "type": "action",
        "name": "Drive_Workflow",
        "steps": [
          {
            "kind": "evaluate",
            "expression": "$LOCAL.test_spec is set"
          },
          {
            "kind": "instruct",
            "instruction": "Create a fresh execution of the tree named in $LOCAL.test_spec.tree with a summary derived from the scenario name. Store the new execution id at $LOCAL.target_execution_id.\nSeed the target execution's $LOCAL from $LOCAL.test_spec.background.initial.local (if present) via `abtree local write <target> <path> <json-value>` for each entry.\nAcknowledge the protocol gate on the target execution (first `abtree next` returns Acknowledge_Protocol — submit success).\nDrive the target execution to completion by walking each `when` step in scenario order. For evaluate steps the agent MUST read every $LOCAL/$GLOBAL path the expression references via `abtree local/global read <target>` before calling `abtree eval <target> true|false`. For instruct steps the agent MUST perform the work named (file I/O, shell, sub-agent — whatever the instruction directs) and write any produced values to $LOCAL via `abtree local write <target> …` before calling `abtree submit <target> success|failure|running`.\nThe driver loop terminates when `abtree next <target>` returns `{status: done}` or `{status: failure}`. If the runner hits an ambiguous instruct it cannot satisfy, submit failure on the *target* execution and continue — the comparison step will then record the failure mode against the spec's expected status."
          }
        ]
      },
      {
        "type": "action",
        "name": "Capture_Trace",
        "steps": [
          {
            "kind": "evaluate",
            "expression": "$LOCAL.target_execution_id is set"
          },
          {
            "kind": "instruct",
            "instruction": "Read the mermaid trace at `.abtree/executions/<$LOCAL.target_execution_id>.mermaid` and store its full contents (verbatim) at $LOCAL.mermaid_diagram. If the file does not exist, submit failure."
          },
          {
            "kind": "instruct",
            "instruction": "Read the target execution's full $LOCAL via `abtree local read <$LOCAL.target_execution_id>` and store the returned object at $LOCAL.final_local.\nRead the target execution's terminal status. Call `abtree next <$LOCAL.target_execution_id>` one more time — the runtime returns `{status: done}` or `{status: failure}` on a completed execution — and store that status string at $LOCAL.final_status."
          }
        ]
      },
      {
        "type": "action",
        "name": "Compare_Assertions",
        "steps": [
          {
            "kind": "evaluate",
            "expression": "$LOCAL.final_local is set and $LOCAL.final_status is set"
          },
          {
            "kind": "instruct",
            "instruction": "For each assertion in $LOCAL.test_spec.scenario.then, compute a record { name, expected, actual, pass }:\n\n  - then.status → name \"status\", expected = scenario.then.status,\n    actual = $LOCAL.final_status, pass = expected == actual.\n\n  - then.local.<key> → name \"local.<key>\", expected = the spec\n    value (treat literal scalars as equality, treat strings\n    starting with \"matches \" / \"starts with \" / \"non-empty\" as\n    their corresponding predicates), actual = $LOCAL.final_local[key],\n    pass = predicate(actual).\n\n  - then.files.<key> → name \"files.<key>\", expected = the spec\n    value, actual = the on-disk reality (file existence,\n    frontmatter scan), pass = predicate(actual). If a\n    files-assertion needs information the runner did not\n    capture, record pass=false with actual=\"not captured\" — do\n    not invent.\n\n  - then.git.* and similar side-effect assertions → same\n    pattern: pass=false with actual=\"not captured\" if the\n    runner did not observe the side effect.\n\nStore the array at $LOCAL.assertions. Do not skip assertions — an unverifiable assertion is a failed assertion, not an absent one."
          },
          {
            "kind": "instruct",
            "instruction": "Compute $LOCAL.overall_result: \"pass\" if every record in $LOCAL.assertions has pass=true, else \"fail\"."
          }
        ]
      },
      {
        "type": "action",
        "name": "Write_Report",
        "steps": [
          {
            "kind": "evaluate",
            "expression": "$LOCAL.assertions is set and $LOCAL.overall_result is set"
          },
          {
            "kind": "instruct",
            "instruction": "Compose a markdown test report with exactly these sections:\n\n  # Test report — <scenario.name>\n\n  **Tree:** <test_spec.tree>\n  **Spec:** <test_path>\n  **Target execution:** <target_execution_id>\n  **Overall:** <overall_result> (uppercased: PASS / FAIL)\n\n  ## Final $LOCAL\n  (table: key | value — rendered from $LOCAL.final_local)\n\n  ## Assertions\n  (table: Name | Expected | Actual | Pass — rendered from $LOCAL.assertions)\n\n  ## Trace\n  ```mermaid\n  <verbatim contents of $LOCAL.mermaid_diagram>\n  ```\n\nWrite it next to the spec, in the same directory as $LOCAL.test_path. The filename is `REPORT__<scenario-stem>__<YYYYMMDDTHHMMSSZ>.md`, where `<scenario-stem>` is the basename of $LOCAL.test_path with the `TEST__` prefix and `.yaml` extension stripped (so `.abtree/trees/hello-world/TEST__morning.yaml` produces `.abtree/trees/hello-world/REPORT__morning__<ts>.md`). Store the resulting path at $LOCAL.report_path."
          },
          {
            "kind": "evaluate",
            "expression": "$LOCAL.report_path is set"
          }
        ]
      }
    ]
  }
}